import { StyleSheet } from 'react-native';

export const globalStyles = StyleSheet.create({
  container: {
    flex: 1,  // Allow the container to use the full available screen
  },
  map: {
    width: '100%',  
    height: '100%', 
  },
});
